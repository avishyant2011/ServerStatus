name: ServerStatus
version: 1.0.0
main: me.serverstatus.notifier.ServerStatus
api-version: 1.20

author: ServerStatus
description: Discord embed notifications for server status

commands:
  sendembed:
    description: Send a test embed
    permission: serverstatus.send

permissions:
  serverstatus.send:
    default: op
